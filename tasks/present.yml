---

- name: Install debian specific package_facts dependency
  become: true
  when: ansible_distribution == "Debian"
  ansible.builtin.package:
    name: python-apt-common
    state: present

- name: Gather package facts
  ansible.builtin.package_facts:

- name: Include setup procedure
  loop: "{{ maya_installers }}"
  loop_control:
    loop_var: "installer"
    label: "{{ installer.name }}"
  ansible.builtin.include_tasks:
    file: software_setup.yml

# - name: Include license manager
#   ansible.builtin.include_tasks:
#     file: license_manager.yml

# - name: Include license manager
#   ansible.builtin.include_tasks:
#     file: maya_install.yml

# TODO streamline install files from a list: requires download url, target location and filter list

...
